@model IEnumerable<FootBallPlayer.Models.Vister>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="zn_section eluid63f0c71a     section-sidemargins    section--no " id="loginForm" style="height:1000px">
    <div class="zn_section_size container zn-section-height--auto zn-section-content_algn--top ">
        <div class="row ">
            <div class="eluid50fcd246      col-md-12 col-sm-12   znColumnElement" id="eluid50fcd246">
                <div class="znColumnElement-innerWrapper-eluid50fcd246 znColumnElement-innerWrapper znColumnElement-innerWrapper--valign-top znColumnElement-innerWrapper--halign-left ">
                    <div class="znColumnElement-innerContent">
                        <div class="zn_text_box eluid7d297e35  zn_text_box-light element-scheme--light">
                            <h1 style="text-align: center;"><span style="font-family: roboto; font-weight: 300;"><span style="font-size: 44px;"><span style="color: #000000;">Visters</span></span></span></h1>
                        </div>
                        <div class="zn_contact_form_container contactForm cf-elm eluidcf4f7ad9 zn-cform cf--light element-scheme--light  ">
                            <table class="table">

                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.FullName)

                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Age)
                                    </th>
                                    <th></th>
                                </tr>

                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>

                                            @Html.DisplayFor(modelItem => item.FullName)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Age)
                                        </td>
                                        <td>
                                            @Html.ActionLink("Edit", "Edit", new { id = item.VisterId }) |
                                            @Html.ActionLink("Details", "Details", new { id = item.VisterId }) |
                                            @Html.ActionLink("Delete", "Delete", new { id = item.VisterId })
                                        </td>
                                    </tr>
                                }

                            </table>

                        </div>
                </div>
            </div>
        </div>
    </div>
 </div>
</section>