@model FootBallPlayer.ViewModels.MediaDetailsViewModel
@using FootBallPlayer.Models

@{
    ViewBag.Title = "VideoDetails";
    Layout = "~/Views/Shared/_PlayerLayout.cshtml";
}

@*<img src="@Model.Photo.Path" alt="Alternate Text" />*@


@*@Html.RenderPartial("Comment", new { id = Model.VideoId, MediaType = 1 });*@

    <div class="row">
        <div class="col-md-7" style=" box-shadow: 0 4px 5px 0 rgba(0,0,0,.14), 0 1px 10px 0 rgba(0,0,0,.12), 0 2px 4px -1px rgba(0,0,0,.2) ;margin-top:5%;margin-right:20px">


            <h3 style="color:red ;text-align:center">Title</h3>

            <iframe width="560" height="315" src="@Model.Video.Url" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe>
            <div style="   ;padding:10px;font-family:cursive">
                <p>@Model.Video.Discribtion</p>
            </div>

        </div>



        <div class="col-md-3" style=" box-shadow: 0 4px 5px 0 rgba(0,0,0,.14), 0 1px 10px 0 rgba(0,0,0,.12), 0 2px 4px -1px rgba(0,0,0,.2);margin-top:5%">
            <h3 style="color:red ;text-align:center">Comment</h3>
            @Html.Action("Create", "Comments", new Comment { MediaId = Model.VideoId, MediaType = 1 })
            @foreach (var item in Model.Comments)
    {
        <div class="col-md-2">
            @item.Text
        </div>
}
        </div>
    </div>
    @*<div class="col-md-12" style="text-align:center;margin-top:20px">
        <p>

            @Html.ActionLink("Back to List", "Videos", new { id = Model.PlayerId })
        </p>
    </div>*@
  
